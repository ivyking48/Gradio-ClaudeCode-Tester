name: gradio-tester
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
    - gradio>=4.0.0
    - gradio_client>=1.0.0
    - playwright>=1.40.0
    - pytest>=7.0.0
    - pytest-asyncio>=0.23.0
    - responses>=0.24.0
